import type { Config } from '@react-router/dev/config'

export default {
    appDirectory: 'rr7-src/app',
    buildDirectory: 'build/react-router',
    basename: '/react-router/',
    ssr: true
} satisfies Config
